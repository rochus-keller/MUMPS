This is a version of testcases/corpus/decus/extracted/games/startrek.rou with each of the 15 routines 
split into separate files (cross-routine references included), so my interpreter can run it.

Run `mumps STARTREK.rou` from the command line, or `DO ^STARTREK` in the REPL.
